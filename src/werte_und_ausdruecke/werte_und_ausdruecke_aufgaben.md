# Aufgaben: Werte und Ausdrücke

## Quiz

### Quiz 1

{{#quiz ../quizzes/werte_und_ausdruecke_01.toml}}

### Quiz 2

{{#quiz ../quizzes/werte_und_ausdruecke_02.toml}}

### Quiz 3

{{#quiz ../quizzes/werte_und_ausdruecke_03.toml}}

### Quiz 4

{{#quiz ../quizzes/werte_und_ausdruecke_04.toml}}

### Quiz 5

{{#quiz ../quizzes/werte_und_ausdruecke_05.toml}}

### Quiz 6

{{#quiz ../quizzes/werte_und_ausdruecke_06.toml}}

### Quiz 7

{{#quiz ../quizzes/werte_und_ausdruecke_07.toml}}

### Quiz 8

{{#quiz ../quizzes/werte_und_ausdruecke_08.toml}}

### Quiz 9

{{#quiz ../quizzes/werte_und_ausdruecke_09.toml}}

## Übungen im Python-Playground

Bearbeite die folgenden Aufgaben im Python-Playground.

### 1. Vorhersage → Ausführen

Schreibe zuerst das Ergebnis auf. Führe dann den Code aus und überprüfe deine Vermutung.

``` python, py-execute
5 - 2 * 3
```

### 2. Verändere die Eingabe

Ändere genau eine Zahl, damit der Ausdruck den Wert `20` hat.

``` python, py-execute
2 + 3 * 4
```

### 3. Ergänze die Lücken

Setze Klammern ein, damit der Ausdruck den Wert `25` hat.

``` python, py-execute
3 + 2 * 5
```

### 4. Fehler finden und reparieren

Entferne genau ein Zeichen, sodass der Ausdruck gültig ist und den Wert `1` hat.

``` python, py-execute
1 + * 1
```

### 5. Prüfe deine Idee

Vergleiche die beiden Ausdrücke und erkläre, warum sie unterschiedliche Ergebnisse haben.

``` python, py-execute
(2 + 3) * 4
```

``` python, py-execute
2 + 3 * 4
```

### 6. Vorhersage → Ausführen (mehrere Schritte)

Der Ausdruck enthält mehrere Rechenschritte. Berechne ihn zuerst per Hand und notiere jeden Zwischenschritt. Überprüfe dein Ergebnis danach im Playground.

``` python, py-execute
2 + 3 * (4 - 1) * 2
```

### 7. Ergänze die Lücken (zwei Klammerpaare)

Setze zwei Klammerpaare so ein, dass der Ausdruck den Wert `15` hat.

``` python, py-execute
2 + 3 * 4 - 1
```

### 8. Fehler finden und reparieren (fehlende Klammer)

Der Ausdruck hat einen SyntaxError, weil eine Klammer fehlt. Ergänze die fehlende Klammer, sodass der Ausdruck gültig ist und den Wert `20` hat.

``` python, py-execute
4 * (2 + 3
```


