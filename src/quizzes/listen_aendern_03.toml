[[questions]]
type = "MultipleChoice"
prompt.prompt = "Was passiert mit `xs`, wenn `ys = xs` ausgeführt wird und danach `ys[2] = False`?"
prompt.distractors = [
  "xs bleibt komplett unverändert, weil ys eine eigene Kopie ist.",
  "Es entsteht ein Fehler, weil zwei Namen nicht auf dieselbe Liste zeigen dürfen.",
  "Nur ys wird geändert, xs bleibt beim alten Wert."
]
answer.answer = "xs ändert sich ebenfalls, weil xs und ys auf dieselbe veränderliche Liste zeigen."
context = "Im Gegensatz zu Integern und Strings wird bei Listen bei y = x keine Kopie erstellt, sondern dieselbe Liste referenziert."
id = "2d3e4f5a-4e3d-4a3a-9e2a-9d1f6b7a5c43"
