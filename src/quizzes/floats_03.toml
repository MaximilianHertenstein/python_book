[[questions]]
type = "MultipleChoice"
prompt.prompt = "Was passiert bei `1.3 + 2` (Float plus Integer)?"
prompt.distractors = [
  "Es entsteht ein Fehler, weil man Float und Integer nicht addieren kann.",
  "Das Ergebnis wird automatisch zu einem Integer gerundet.",
  "Nur der Nachkommateil wird berücksichtigt."
]
answer.answer = "Das Integer wird zuerst automatisch in einen Float umgewandelt, danach wird ganz normal addiert."
context = "Bei Operationen zwischen Integer und Float wird das Integer automatisch in einen Float konvertiert."
id = "3e4f5a6b-5e3d-4a3a-9e2a-9d1f6b7a6c53"
